server:
  port: 8000
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      mvc:
        routes:
          - id: feed-service
            uri: http://localhost:8080
            predicates:
              - Path=/**
